import { PowerWithMultiParamsPipe } from './power-with-multi-params.pipe';

describe('PowerWithMultiParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerWithMultiParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
